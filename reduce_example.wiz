proc main()
    int x;
    int y;
    int z;
    int w;

    bool a;
    bool b;
    bool c;

    write 1 + x + y + 6;
    write 1 + ((x + y) + 6);
    write (2 + (x + (y + (z + 5))));
    write (((2 + x) + y*x) + 5);
    write ---x - (y - (z - w));
    write not (not ((not a) and (not b))) and not (not (not c)) and not false;
    write not (not ((not a) and (not b))) and not (not (not c)) and not true;
end
